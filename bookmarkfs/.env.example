# Optional: pre-fill the extension’s “Site URL” when you build the bundle.
# Use the same value as NEXT_PUBLIC_APP_URL on Vercel (e.g. https://your-app.vercel.app)
# or http://localhost:3000 for local dev.
#
# WOXBIN_DEFAULT_SITE_URL=https://your-deployment.vercel.app
